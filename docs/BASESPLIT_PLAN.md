# TileEntityMekanism Atomic Base-Split — verified execution plan

From the `s-machines-basesplit-exec` workflow (5 scouts/designer/3 verifiers; all 3 verdicts sound-with-fixes, NBT-identity lens verified point-by-point). Fixes folded in: handleUpdateTag super-shim, ambientTemperature drop-final, radiationScale private->protected, MachineEnergyContainer.supportsUpgrade -> seam.

## Overview
ATOMIC base-split of TileEntityMekanism.java (1639 lines, verified). Final hierarchy: TileEntityUpdateable(:common) <- TileEntityMekanismBase(:common, NEW abstract, ~1500 loader-clean lines) <- CapabilityTileEntity(neoforge, re-parented) <- TileEntityMekanism(neoforge LEAF, FQN preserved). The leaf retains: 3 component fields (upgrade/frequency/security), the 5 NBT/component leaf-hook overrides (ContainerType.TYPES iteration only), addContainerTrackers + trackLastEnergy(MekanismContainer), the IComputerTile family (@ComputerMethod-annotated + security/computer methods), getName/getDisplayName/getContainerDescription trio (RegistryUtils), wrench/gui cluster, config-card methods, getComponent/recalculateUpgrades/getUpgradedMaxEnergy/getUpgradedEnergyPerTick, updateRadiationScale/getRadiationScale, getDirection (annotated), the 2 static tick drivers (verbatim), handleUpdateTag, and the ctor (getInitial* + buildAndRegisterManagers + the 3 leaf-component constructions + the AttributeSound-named soundEvent init). CapabilityTileEntity gains the 10 manager-reading capability-seam overrides (its 5 manager fields are unchanged). The base owns everything else, reaching managers ONLY through the 10 seam methods (no-op/empty defaults on base). MachineEnergyContainer moves to :common alongside the base (ConstantPredicates.alwaysTrue() reroute + TILE bound widened to TileEntityMekanismBase). Two MekanismConfig reads resolved via seams: delaySupplier field reassigned in leaf ctor, and a new ITileSoundService.machineSoundsEnabled() boolean. NBT byte-identity is preserved because the super-chain is unchanged (base inserted between TileEntityUpdateable and CapabilityTileEntity, neither of which overrides any NBT method) and every leaf-hook is inserted at the EXACT original interleave point of the ContainerType.TYPES loop.

## NBT byte-identity argument (load-bearing)
Point-by-point proof of save/load/data-component byte-identity. (1) SUPER-CHAIN UNCHANGED: inserting TileEntityMekanismBase between TileEntityUpdateable and CapabilityTileEntity does not change super-call order because CapabilityTileEntity overrides NONE of saveAdditional/loadAdditional/collectImplicitComponents/applyImplicitComponents/getRemapEntries/removeComponentsFromTag/writeReducedUpdatedTag (verified: CapabilityTileEntity.java overrides only setRemoved/clearRemoved/onDataPacket). So for each NBT method the chain is exactly base.method() -> super == TileEntityUpdateable.method(), identical to today's leaf -> TileEntityUpdateable. The leaf-hook overrides call super.<theBaseMethod> which then calls TileEntityUpdateable — same order. (2) saveAdditional ORDER (current leaf lines 730-755): super.saveAdditional; putBoolean(REDSTONE); components loop component.write; writeSustainedData (CONTROL_TYPE); [TYPES loop type.saveTo]; isActivatable -> putBoolean(ACTIVE_STATE)+putInt(UPDATE_DELAY); supportsComparator -> putInt(CURRENT_REDSTONE); isNameable -> storeNullable(CUSTOM_NAME). SPLIT: base.saveAdditional runs lines 731-736 (super, REDSTONE, components loop, writeSustainedData) then calls protected hook saveAdditionalContainers(output) [base default no-op] then lines 744-754 (isActivatable, comparator, customName). Leaf override saveAdditionalContainers runs ONLY the TYPES loop (lines 738-742). Insertion point is byte-identical: the TYPES loop sits between writeSustainedData and the isActivatable block exactly as today. (3) loadAdditional ORDER (lines 702-726): super; getBooleanOr(REDSTONE); components loop component.read; if supportsUpgrades recalculateUpgrades(SPEED); readSustainedData; [TYPES loop type.readFrom]; isActivatable reads; comparator read; isNameable read. SPLIT: base runs 703-711 (super, REDSTONE, components loop, recalculateUpgrades-via-hook, readSustainedData) then loadAdditionalContainers(input) hook [leaf: TYPES loop 712-716] then 717-726. recalculateUpgrades is leaf-coupled (getComponent) so base reaches it via the existing protected recalculateUpgrades(Upgrade) method which the leaf overrides — the CALL stays at line 708-710 position; same order. (4) collectImplicitComponents ORDER (847-867): super; components loop collectImplicitComponents; [TYPES loop copyFromTile]; FilterAware branch; REDSTONE_CONTROL set. SPLIT: base runs super + components loop then trailing hook collectImplicitComponentsLeaf(builder) [leaf: TYPES loop + FilterAware + builder.set(REDSTONE_CONTROL, controlType)]. The leaf reads protected controlType. Order identical: TYPES/FilterAware/redstone all AFTER the components loop. (5) applyImplicitComponents ORDER (774-805): super; isNameable->setCustomName; components loop; supportsUpgrades recalculate loop; [TYPES copyToTile]; FilterAware; REDSTONE_CONTROL via setControlType. SPLIT: base runs super + customName + components loop + recalculate loop (via the protected recalculateUpgrades hook + getSupportedUpgrade) then trailing hook applyImplicitComponentsLeaf(input) [leaf: TYPES copyToTile + FilterAware + setControlType(getOrDefault(REDSTONE_CONTROL))]. setControlType/getControlType are public so no field promotion needed on this path. Order identical. (6) getRemapEntries (809-823): base runs super + components loop (mutating the SAME list returned by super) then hook addRemapEntriesLeaf(remapEntries) [leaf: TYPES adds + FILTER_AWARE add] then returns the SAME list. List identity + append order preserved exactly. (7) removeComponentsFromTag (828-844): NO leaf coupling (only ITileComponent + SerializationConstants keys + supportsComparator/isActivatable/supportsRedstone booleans) -> moves VERBATIM to base, no hook. (8) writeReducedUpdatedTag (928-935): components loop + putFloat(RADIATION) — fully base-clean, moves verbatim to base. (9) FIELD READ/WRITE ORDER: all serialized fields (redstone, controlType, currentActive, updateDelay, currentRedstoneLevel, customName, radiationScale) move to the base, read/written at the identical positions; controlType promoted private->protected so the leaf collect/track overrides read the raw field at the same point. (10) handleUpdateTag (938-944, NeoForge ValueInput co-add, absent on vanilla) stays on the leaf — calls super.loadAdditional + components loop + RADIATION read; cannot @Override in :common but its body order is unchanged. NET: every NBT key is written/read in the identical sequence; NeoForge on-disk and network bytes are unchanged.

=== fieldVisibilityChanges ===
 - controlType: private -> protected (leaf collectImplicitComponentsLeaf reads raw at orig line 866; addContainerTrackers reads+writes raw at 881; getControlType annotated leaf method reads it)
 - redstoneLastTick: private -> protected (addContainerTrackers leaf lambda writes raw at orig line 883; tickServer static reads/writes it but that is leaf anyway)
 - lastEnergyTracker: private -> protected (KEEP final; addContainerTrackers leaf reads raw at orig line 924)
 - cachedDirection: private -> protected (leaf @ComputerMethod getDirection reads+writes it at orig 978-984; setBlockState on base also uses it but base can keep private only if getDirection moves too — getDirection is annotation-pinned to leaf, so promote)
 - currentRedstoneLevel: private -> protected (leaf @ComputerMethod getCurrentRedstoneLevel reads it at orig 1119; base load/save/tick also use it)
 - supportsComputers, hasChunkloader, nameable: private -> protected (leaf setSupportedTypes writes them at orig 301-303)
 - supportsUpgrades, canBeUpgraded, isDirectional, supportsRedstone, hasSound, hasGui, hasBounding, hasSecurity, isActivatable, supportsComparator, activeAttribute: private -> protected (leaf setSupportedTypes writes all of them at orig 290-300; setSupportedTypes stays on leaf because lines 301-303 name Mekanism.hooks/IChunkLoader/AttributeGui)
 - soundEvent: loses 'final' (was protected final) -> protected (reassigned in leaf ctor instead of base-ctor-final-init, to avoid naming AttributeSound in :common)
 - 8 capability seam methods (hasInventory, canHandleFluid, canHandleEnergy, canHandleHeat, getInventorySlots, getFluidTanks, getEnergyContainers, getHeatCapacitors): lose 'public final' -> plain public (so CapabilityTileEntity can override the base declaration; canHandleChemicals + getChemicalTanks were already non-final)

=== newSeams ===
 - ITileSoundService.machineSoundsEnabled():boolean — NEW method on existing :common seam; NeoTileSoundService returns MekanismConfig.client.enableMachineSounds.get(); FabricTileSoundService returns false. Replaces the inline MekanismConfig.client.enableMachineSounds.get() in updateSound() (orig line 1521) so updateSound lives fully in :common
 - protected void saveAdditionalContainers(ValueOutput output) — base no-op default; leaf override = the ContainerType.TYPES saveTo loop (orig 738-742)
 - protected void loadAdditionalContainers(ValueInput input) — base no-op default; leaf override = the ContainerType.TYPES readFrom loop (orig 712-716)
 - protected void applyImplicitComponentsLeaf(DataComponentGetter input) — base no-op default; leaf override = TYPES copyToTile + FilterAware + setControlType(REDSTONE_CONTROL) (orig 791-805)
 - protected void collectImplicitComponentsLeaf(DataComponentMap.Builder builder) — base no-op default; leaf override = TYPES copyFromTile + FilterAware + builder.set(REDSTONE_CONTROL, controlType) (orig 854-867)
 - protected void addRemapEntriesLeaf(List<DataComponentType<?>> remapEntries) — base no-op default; leaf override = TYPES + FILTER_AWARE adds, mutating the SAME list (orig 814-822)
 - protected boolean isFullyMuffled() — base default returns false; leaf override = getComponent().getUpgrades(MUFFLING) body (orig 1556-1561). Lets updateSound() (base) call it without naming TileComponentUpgrade
 - delaySupplier field-reassign seam — base `protected IntSupplier delaySupplier = () -> 0;`; leaf ctor reassigns = MekanismConfig.general.blockDeactivationDelay
 - soundEvent field-reassign seam — base `protected Supplier<SoundEvent> soundEvent;` (non-final); leaf ctor assigns the AttributeSound-derived value
 - The 10 capability methods (hasInventory/canHandleChemicals/canHandleFluid/canHandleEnergy/canHandleHeat + getInventorySlots/getChemicalTanks/getFluidTanks/getEnergyContainers/getHeatCapacitors): base no-op/empty defaults, CapabilityTileEntity manager-reading overrides (these are the entire capability seam; no NEW method names — the existing public interface getters/predicates ARE the seam)
 - protected TileEntityMekanismBase(BlockEntityType<?>, BlockPos, BlockState) — new base ctor forwarding to TileEntityUpdateable; CapabilityTileEntity's existing super(type.get(),pos,state) now resolves to it with no source edit

=== STEPS ===

--- Step 1: Add machineSoundsEnabled() to the ITileSoundService seam + both impls
   files: ['/Users/goddv/Development/Mekanism-Fabric/common/src/main/java/mekanism/common/tile/base/ITileSoundService.java', '/Users/goddv/Development/Mekanism-Fabric/neoforge/src/main/java/mekanism/common/tile/base/NeoTileSoundService.java', '/Users/goddv/Development/Mekanism-Fabric/fabric/src/main/java/mekanism/fabric/tile/FabricTileSoundService.java']
   In common/.../tile/base/ITileSoundService.java add: `boolean machineSoundsEnabled();` (after isActiveSound). In neoforge NeoTileSoundService.java add override `public boolean machineSoundsEnabled(){ return mekanism.common.config.MekanismConfig.client.enableMachineSounds.get(); }` (add the import). In fabric FabricTileSoundService.java add override `public boolean machineSoundsEnabled(){ return false; }` (mirrors its existing sound no-op; ACTIVE blockstate still animates). This lets updateSound() live fully in :common.

--- Step 2: Move MachineEnergyContainer to :common with reroute + bound widening
   files: ['/Users/goddv/Development/Mekanism-Fabric/neoforge/src/main/java/mekanism/common/capabilities/energy/MachineEnergyContainer.java', '/Users/goddv/Development/Mekanism-Fabric/common/src/main/java/mekanism/common/capabilities/energy/MachineEnergyContainer.java']
   git mv neoforge/.../capabilities/energy/MachineEnergyContainer.java -> common/.../capabilities/energy/MachineEnergyContainer.java. Edit: drop `import mekanism.api.functions.ConstantPredicates;`, add `import mekanism.api.functions.ConstantPredicatesBase;`; change line 21 `ConstantPredicates.alwaysTrue()` -> `ConstantPredicatesBase.alwaysTrue()`. Leave the bare `notExternal`/`internalOnly` refs (lines 21,26) UNTOUCHED — they are inherited public-static fields of BasicEnergyContainer (verified :common). Change the type bound on the class decl (line 17) and BOTH factory methods (input line 19, internal line 24) and validateBlock (line 29 param) from `TileEntityMekanism` -> `TileEntityMekanismBase`; change `import ...tile.base.TileEntityMekanism` -> `import ...tile.base.TileEntityMekanismBase`. Callers (getInitialEnergyContainers overrides in leaf/prefab tiles) pass concrete leaf tiles which IS-A base — assignment-compatible, no caller edits.

--- Step 3: Create TileEntityMekanismBase (:common, NEW abstract) — the loader-clean core
   files: ['/Users/goddv/Development/Mekanism-Fabric/common/src/main/java/mekanism/common/tile/base/TileEntityMekanismBase.java', '/Users/goddv/Development/Mekanism-Fabric/neoforge/src/main/java/mekanism/common/tile/base/TileEntityMekanism.java']
   New file common/.../tile/base/TileEntityMekanismBase.java. Decl: `public abstract class TileEntityMekanismBase extends TileEntityUpdateable implements ITileDirectional, IConfigCardAccess, ITileActive, ITileSound, ITileRedstone, ITierUpgradable, IComparatorSupport, IMekanismFluidHandler, IMekanismStrictEnergyHandler, ITileHeatHandler, IMekanismChemicalHandler, IEnergyBufferMultiplier, Nameable` (13 :common/vanilla interfaces; OMIT IFrequencyHandler/ISecurityTile/IComputerTile/ITileFilterHolder/ITileRadioactive/ITileUpgradable/IMekanismInventory/ITrackableContainer — those stay on leaf). Ctor: `protected TileEntityMekanismBase(BlockEntityType<?> type, BlockPos pos, BlockState state){ super(type, pos, state); }`. MOVE these 32 fields verbatim from the leaf (decl lines 153-254): playersUsing, ticker, components, blockProvider, supportsComparator, supportsComputers, supportsUpgrades, supportsRedstone, canBeUpgraded, isDirectional, isActivatable, activeAttribute, hasBounding, hasSecurity, hasSound, hasGui, hasChunkloader, nameable, customName, containerDescription(see step 8), syncMasterToBounding, cachedDirection, facingSupplier, redstone(protected), redstoneLastTick, controlType, currentRedstoneLevel, updateComparators, radiationScale, lastEnergyTracker(final), ambientTemperature(protected final), currentActive, updateDelay, delaySupplier(see step 9), soundEvent(protected final,see step 7), lastSoundEvent, activeSound, playSoundCooldown. PROMOTE to protected: controlType, redstoneLastTick, lastEnergyTracker (still final) — leaf collect/track overrides read them raw. MOVE these base-clean methods (full list, with reroutes noted): getBlockHolder, setSupportedTypes(SPLIT, step 6), presetVariables, addComponent, getComponents, getCustomName, setCustomName, isNameable, markDirtyComparator, notifyComparatorChange, encodeExtraContainerData, tickClient-delegated onUpdateClient/onUpdateServer hooks, open, close, setRemoved(the components-loop+sound half — see step 6), preRemoveSideEffects, resyncMasterToBounding, setBlockState, loadAdditional(SPLIT), saveAdditional(SPLIT), writeSustainedData, readSustainedData, applyImplicitComponents(SPLIT), getRemapEntries(SPLIT), removeComponentsFromTag(verbatim), collectImplicitComponents(SPLIT), writeReducedUpdatedTag(verbatim), onNeighborChange, onAdded, the 5 getInitial* hooks, getInventorySlots/getChemicalTanks/getFluidTanks/getEnergyContainers/getHeatCapacitors (no-op/empty seam impls — step 5), the 5 canHandle*/hasInventory (no-op seam impls — step 5), onContentsChanged, apply/collect Inventory/Chemical/Energy/Heat data helpers (the :common-typed ones — NOT applyFluidTanks/collectFluidTanks which stay on CapabilityTileEntity), shouldDumpRadiation, insertEnergy(2 overrides), trackLastEnergy(long,Action,long) private, getInputRate, getAmbientTemperature, getAdjacent, getAdjacentUnchecked(uses ICapabilityExposureService seam), parseUpgradeData(reroute Mekanism.logger->MekanismAPIBase.logger), invalidateDirectionCaches, setControlType, supportedOrNextType, isPowered, wasPowered, updatePower, isRedstoneActivated, canFunction, getRedstoneLevel(reroute MekanismUtils.redstoneLevelFromContents->MekanismUtilsBase), makesComparatorDirty, getListener, getSupportedUpgrade, getActive, getClientActive, setActive, canPlaySound, updateSound(uses machineSoundsEnabled seam from step 1), isFullyMuffled(SPLIT hook — step 4), getTotalEnergy(ToLongFunction) private helper. Add imports for MekanismUtilsBase, MekanismAPIBase, ConstantPredicatesBase as needed. Base must name ZERO of: ContainerType, MekanismContainer/Syncable*/SyncMapper, TileComponent*, MekanismConfig, MekanismDataComponents, FilterManager/FilterAware, RegistryUtils, WorldUtils, BlockSecurityUtils, @ComputerMethod, Capabilities, ItemConfigurator/ItemConfigurationCard, MekanismTags.

--- Step 4: isFullyMuffled / recalculateUpgrades / getComponent protected hooks (base default + leaf override)
   files: ['/Users/goddv/Development/Mekanism-Fabric/common/src/main/java/mekanism/common/tile/base/TileEntityMekanismBase.java', '/Users/goddv/Development/Mekanism-Fabric/neoforge/src/main/java/mekanism/common/tile/base/TileEntityMekanism.java']
   isFullyMuffled (leaf body names getComponent()->TileComponentUpgrade): declare on BASE as `protected boolean isFullyMuffled(){ return false; }`; updateSound (now base) calls it. LEAF overrides with the verbatim body (lines 1556-1561) reading getComponent(). recalculateUpgrades(Upgrade) is CALLED from base loadAdditional/applyImplicitComponents but its body names getComponent()/MachineEnergyContainer cast — keep its DECLARATION on the LEAF (verbatim lines 1160-1176); BASE references it via an abstract/overridable protected method `protected void recalculateUpgrades(Upgrade upgrade){}` (no-op default) OR declare recalculateUpgrades on base as overridable and let leaf override. Since recalculateUpgrades names MachineEnergyContainer (now :common) + getEnergyContainers (base seam), its ONLY leaf coupling is none after MachineEnergyContainer moves — VERIFY: recalculateUpgrades body uses getEnergyContainers + MachineEnergyContainer + Upgrade only, all :common after step 2 -> it can MOVE TO BASE entirely. getComponent itself (returns TileComponentUpgrade) STAYS leaf. getSupportedUpgrade is base-clean (Attribute + AttributeUpgradeSupport — verify AttributeUpgradeSupport is :common; if neoforge, keep getSupportedUpgrade on leaf and have applyImplicitComponents base-hook call it). getUpgradedMaxEnergy + getUpgradedEnergyPerTick STAY LEAF (MekanismUtils.getMaxEnergy/getEnergyPerTick have NO MekanismUtilsBase twin — verified). Base declares them via the existing IEnergyBufferMultiplier/self-hook contract: getUpgradedMaxEnergy/getUpgradedEnergyPerTick are interface methods the leaf implements; MachineEnergyContainer calls them through the tile field (base type) so the base must DECLARE them (abstract or default). Make base provide `default`/overridable `long getUpgradedMaxEnergy(long base){ return base; }` and `long getUpgradedEnergyPerTick(long base){ return base; }`? NO — they are on ITileUpgradable/IEnergyBufferMultiplier. Cleanest: declare both as overridable protected/public on base returning base unchanged; LEAF overrides verbatim with the MekanismUtils calls. This keeps MachineEnergyContainer (calling tile.getUpgradedMaxEnergy) resolving against the base type.

--- Step 5: Capability seam: base no-op/empty defaults + drop final on 8
   files: ['/Users/goddv/Development/Mekanism-Fabric/common/src/main/java/mekanism/common/tile/base/TileEntityMekanismBase.java']
   On BASE declare the 10 seam methods with safe defaults (these satisfy the 4 :common handler interfaces the base implements; TileEntityTransmitter inherits them inertly): `public boolean hasInventory(){return false;}`, `public boolean canHandleChemicals(){return false;}`, `public boolean canHandleFluid(){return false;}`, `public boolean canHandleEnergy(){return false;}`, `public boolean canHandleHeat(){return false;}`, `public List<IInventorySlot> getInventorySlots(@Nullable Direction side){return Collections.emptyList();}`, and same `Collections.emptyList()` for getChemicalTanks/getFluidTanks/getEnergyContainers/getHeatCapacitors. NONE may be `final` on the base (CapabilityTileEntity must override). VERIFIED zero TileEntityMekanism tile subclasses re-override these, so dropping final is behaviorally inert.

--- Step 6: Split the 5 NBT/component methods + setSupportedTypes + setRemoved across base/leaf
   files: ['/Users/goddv/Development/Mekanism-Fabric/common/src/main/java/mekanism/common/tile/base/TileEntityMekanismBase.java', '/Users/goddv/Development/Mekanism-Fabric/neoforge/src/main/java/mekanism/common/tile/base/TileEntityMekanism.java']
   NBT SPLITS (base runs the body up to/around the insertion point, calls a protected hook, leaf overrides hook with ONLY the ContainerType.TYPES portion): saveAdditional -> base lines 731-736 + `saveAdditionalContainers(output)` hook + lines 744-754; leaf `protected void saveAdditionalContainers(ValueOutput output)` = lines 738-742 TYPES loop. loadAdditional -> base 703-711 (recalculateUpgrades now base after step 4) + `loadAdditionalContainers(input)` hook + 717-726; leaf hook = 712-716 TYPES loop. applyImplicitComponents -> base 775-789 + trailing `applyImplicitComponentsLeaf(input)`; leaf hook = 791-805 (TYPES copyToTile + FilterAware + setControlType). collectImplicitComponents -> base 848-853 (super+components loop) + trailing `collectImplicitComponentsLeaf(builder)`; leaf hook = 854-867 (TYPES copyFromTile + FilterAware + builder.set(REDSTONE_CONTROL,controlType) reading protected controlType). getRemapEntries -> base 810-813 (super+components loop on the SAME list) + `addRemapEntriesLeaf(remapEntries)` + return; leaf hook = 814-822. Base hook defaults are all no-op/empty. setSupportedTypes (288-304): lines 301 (Mekanism.hooks), 302 (IChunkLoader instanceof), 303 (AttributeGui) are leaf-coupled. KEEP setSupportedTypes on the LEAF (it is called from the leaf ctor anyway); it writes the base booleans supportsComputers/hasChunkloader/nameable -> PROMOTE those 3 from private to protected. The other base booleans it sets are also base fields but reachable since setSupportedTypes is leaf and base fields it writes must be protected — promote supportsUpgrades/canBeUpgraded/isDirectional/supportsRedstone/hasSound/hasGui/hasBounding/hasSecurity/activeAttribute/isActivatable/supportsComparator to protected too (setSupportedTypes on leaf writes all of them). setRemoved (642-651): base portion = super + components-invalidate loop + the `if(isRemote()&&hasSound()) updateSound()` (all base-clean now) -> moves to base entirely (CapabilityTileEntity.setRemoved also exists and chains super correctly; base.setRemoved super-calls CapabilityTileEntity.setRemoved). VERIFY chain: leaf had setRemoved overriding CapabilityTileEntity.setRemoved; now base.setRemoved overrides CapabilityTileEntity.setRemoved -> but base is SUPERCLASS of CapabilityTileEntity! Hierarchy is base <- CapabilityTileEntity, so CapabilityTileEntity.setRemoved overrides base.setRemoved and calls super.setRemoved()==base.setRemoved. Order: CapabilityTileEntity.setRemoved (invalidateAll) -> super -> base.setRemoved (components loop + sound) -> super -> TileEntityUpdateable. This MATCHES today (leaf.setRemoved called super==CapabilityTileEntity.setRemoved first). Correct.

--- Step 7: soundEvent final-field ctor seam
   files: ['/Users/goddv/Development/Mekanism-Fabric/common/src/main/java/mekanism/common/tile/base/TileEntityMekanismBase.java', '/Users/goddv/Development/Mekanism-Fabric/neoforge/src/main/java/mekanism/common/tile/base/TileEntityMekanism.java']
   soundEvent is `protected final Supplier<SoundEvent>` initialized in the leaf ctor line 285 `= hasSound() ? Attribute.getOrThrow(blockProvider, AttributeSound.class).getSound() : null` which names AttributeSound (leaf). Because it is FINAL it must be set in the ctor, and the base ctor cannot name AttributeSound. Resolution: add a protected abstract hook `protected abstract Supplier<SoundEvent> initSoundEvent();` is wrong (called before subclass init). Instead: keep soundEvent declared+assigned on the BASE but supply the value via a protected method the base ctor calls — BUT base ctor runs before leaf field init. CLEANEST: declare soundEvent as `protected final Supplier<SoundEvent> soundEvent` on base, assign in the BASE ctor from a protected overridable `protected Supplier<SoundEvent> resolveSoundEvent(){ return null; }`. PROBLEM: base ctor calls resolveSoundEvent before leaf is constructed, and the override would name AttributeSound. Since AttributeSound resolution only needs blockProvider+hasSound (both known at base-ctor time IF blockProvider/hasSound are passed/set first), but AttributeSound is leaf-only. ALTERNATIVE (recommended): make soundEvent NON-final on the base (`protected Supplier<SoundEvent> soundEvent;`) and assign it in the LEAF ctor after super() at the existing line-285 position. This is the same field-reassign pattern as delaySupplier and avoids any AttributeSound naming in :common. Behaviorally identical (assigned once, before any sound tick). Note this is the ONE field that loses `final` for the split.

--- Step 8: getName/getDisplayName/getContainerDescription trio + containerDescription field stay on leaf
   files: ['/Users/goddv/Development/Mekanism-Fabric/neoforge/src/main/java/mekanism/common/tile/base/TileEntityMekanism.java', '/Users/goddv/Development/Mekanism-Fabric/common/src/main/java/mekanism/common/tile/base/TileEntityMekanismBase.java']
   getContainerDescription (442-447) names RegistryUtils.getName (STAY-ON-LEAF, no :common twin verified). getDisplayName calls getContainerDescription; getName is clean but splitting the naming trio across the RegistryUtils boundary adds a needless hook. KEEP getName, getDisplayName, getContainerDescription, and the containerDescription field ALL on the LEAF. customName field stays on BASE (used by base load/save customName serialization + getCustomName/setCustomName). The leaf getName/getDisplayName read base customName via getCustomName()/hasCustomName() (public) — no promotion. hasCustomName() (Nameable default) is base-clean.

--- Step 9: delaySupplier field seam
   files: ['/Users/goddv/Development/Mekanism-Fabric/common/src/main/java/mekanism/common/tile/base/TileEntityMekanismBase.java', '/Users/goddv/Development/Mekanism-Fabric/neoforge/src/main/java/mekanism/common/tile/base/TileEntityMekanism.java']
   On BASE declare `protected IntSupplier delaySupplier = () -> 0;` (loader-clean default; setActive reads it but setActive is never called during construction). In the LEAF ctor, after super() and field setup, add `delaySupplier = MekanismConfig.general.blockDeactivationDelay;` (the verbatim former initializer; CachedIntValue implements IntSupplier). This keeps setActive 100% in the base and NeoForge runtime byte-identical (the leaf reassignment runs before any setActive call).

--- Step 10: Re-parent CapabilityTileEntity + add the 10 verbatim manager-reading overrides
   files: ['/Users/goddv/Development/Mekanism-Fabric/neoforge/src/main/java/mekanism/common/tile/base/CapabilityTileEntity.java']
   Change CapabilityTileEntity decl (line 52) `extends TileEntityUpdateable` -> `extends TileEntityMekanismBase`. Its ctor `CapabilityTileEntity(ITileHolder<?> type, BlockPos pos, BlockState state){ super(type.get(), pos, state); }` is UNCHANGED — super(BlockEntityType,pos,state) now resolves to the new base ctor (verified ITileHolder.get() returns the BlockEntityType supplier; type.get() yields BlockEntityType<?>). Add the 10 override methods reading the 5 manager fields VERBATIM from the current leaf bodies, preserving each null-check polarity: `@Override public boolean hasInventory(){return itemHandlerManager!=null && itemHandlerManager.canHandle();}` (and canHandleChemicals/canHandleFluid/canHandleEnergy/canHandleHeat identically); `@Override public List<IInventorySlot> getInventorySlots(@Nullable Direction side){return itemHandlerManager!=null?itemHandlerManager.getContainers(side):Collections.emptyList();}`, `getChemicalTanks` = `chemicalHandlerManager==null?Collections.emptyList():chemicalHandlerManager.getContainers(side)` (note reversed polarity), `getFluidTanks/getEnergyContainers/getHeatCapacitors` = `manager!=null?manager.getContainers(side):Collections.emptyList()`. Add imports: List, Collections, IInventorySlot, IChemicalTank, IExtendedFluidTank(already), IEnergyContainer, IHeatCapacitor. buildAndRegisterManagers/addCapabilityResolvers/the invalidate* family/applyFluidTanks/collectFluidTanks/getAdjacentHeatHandler/onDataPacket UNCHANGED.

--- Step 11: Reshape the leaf TileEntityMekanism: decl, retained members, hook overrides
   files: ['/Users/goddv/Development/Mekanism-Fabric/neoforge/src/main/java/mekanism/common/tile/base/TileEntityMekanism.java']
   Decl (line 146-148) -> `public abstract class TileEntityMekanism extends CapabilityTileEntity implements IFrequencyHandler, ISecurityTile, IComputerTile, ITileFilterHolder, ITileRadioactive, ITileUpgradable, IMekanismInventory, ITrackableContainer` (8 neoforge-only interfaces; the 13 :common ones are inherited via the base). FQN preserved. RETAIN on leaf: frequencyComponent+upgradeComponent+securityComponent fields + containerDescription field; the ctor (with delaySupplier+soundEvent reassignments from steps 7/9, and the 3 component constructions lines 278-285); setSupportedTypes (step 6); getName/getDisplayName/getContainerDescription (step 8); the 5 NBT leaf-hook overrides saveAdditionalContainers/loadAdditionalContainers/applyImplicitComponentsLeaf/collectImplicitComponentsLeaf/addRemapEntriesLeaf (step 6); addContainerTrackers(871-921, reads protected controlType/redstoneLastTick/lastEnergyTracker) + trackLastEnergy(MekanismContainer)(923-925); handleUpdateTag(938-944); the static tickServer(576-632)+tickClient(567-574) VERBATIM; getFrequencyComponent; getSecurity+onSecurityChanged; writeConfigurationData/setConfigurationData/getConfigurationDataType/configurationDataSet; getComputerName/validateSecurityIsPublic/getComputerMethods + all @ComputerMethod methods (getTotalEnergy/getTotalMaxEnergy/getTotalEnergyNeeded/getTotalEnergyFilledPercentage/setRedstoneMode/getDirection/getControlType/getCurrentRedstoneLevel — @ComputerMethod is neoforge, verified); getComponent; getUpgradedMaxEnergy/getUpgradedEnergyPerTick (no MekanismUtilsBase twin); updateRadiationScale/getRadiationScale; isFullyMuffled override(step 4); tryWrench/tryWrenchRotate/tryWrenchDismantle/openGui (WorldUtils/Capabilities/ItemConfigurator). The @ComputerMethod-annotated getDirection/getControlType/getCurrentRedstoneLevel keep their VERBATIM bodies on the leaf (their logic reads base fields cachedDirection/controlType/currentRedstoneLevel — promote controlType already done; cachedDirection+currentRedstoneLevel must be readable: getDirection writes cachedDirection so it needs the field — PROMOTE cachedDirection to protected; getCurrentRedstoneLevel reads currentRedstoneLevel — PROMOTE currentRedstoneLevel to protected). setControlType/getControlType: getControlType is annotated->leaf; setControlType is base. controlType protected covers both. DELETE from the leaf every member that moved to base.

--- Step 12: Compile-gate NeoForge (must stay green/byte-identical) then Fabric, then full build
   files: ['/Users/goddv/Development/Mekanism-Fabric/neoforge/src/main/java/mekanism/common/tile/base/TileEntityMekanism.java', '/Users/goddv/Development/Mekanism-Fabric/common/src/main/java/mekanism/common/tile/base/TileEntityMekanismBase.java', '/Users/goddv/Development/Mekanism-Fabric/neoforge/src/main/java/mekanism/common/tile/base/CapabilityTileEntity.java']
   Run `./gradlew :neoforge:compileJava` first — it is the byte-identity gate (89 namers + static method-refs + ContainerType refs). Then `./gradlew :common:compileJava` and `./gradlew :fabric:compileJava`. Then `./gradlew build`. Fix any field-visibility/import fallout. Confirm MekanismTileEntityTypes + GeneratorsTileEntityTypes still resolve TileEntityMekanism::tickServer/tickClient. Confirm no neoforge subclass of MachineEnergyContainer re-narrows TILE to a leaf-only method.

=== outOfScope ===
 - Fabric energy-tile / BlockEntityType registration on Fabric (real machine tiles registered + ticking on Fabric) — FOLLOW-UP, not this commit. This commit only makes the base loader-clean and movable; it does not register any tile on Fabric.
 - Hoisting MekanismUtils.getMaxEnergy / getEnergyPerTick into MekanismUtilsBase — NOT done here; getUpgradedMaxEnergy/getUpgradedEnergyPerTick stay on the leaf as overridable methods. A future task can hoist these and move the two methods to base.
 - Moving MekanismConfig facade (client/general aggregator) to :common — out of scope; resolved via the two field/seam workarounds (machineSoundsEnabled + delaySupplier reassign).
 - Moving the IComputerTile/@ComputerMethod machinery to :common — out of scope; all annotated methods stay on the leaf.
 - Moving frequency/security/upgrade TileComponent* classes to :common — out of scope; they stay on the leaf and pin their 3 fields + getComponent/getFrequencyComponent/getSecurity to the leaf.
 - getName/getDisplayName/getContainerDescription staying on the leaf due to RegistryUtils — a future RegistryUtils :common twin could move the naming trio to base, but not in this commit.
 - TileEntityTransmitter behavior changes — none intended; it is only transitively re-parented and inherits the base no-op seam defaults inertly.

=== risks ===
 - RUNTIME (needs in-game test): NeoForge sound playback — updateSound() now routes the config gate through ITileSoundService.machineSoundsEnabled() instead of inline MekanismConfig.client.enableMachineSounds.get(). Confirm machine looping sounds still start/stop/mute identically in-game (the service INSTANCE must be non-null on NeoForge — it is, via META-INF/services). One extra virtual call per client sound tick, negligible.
 - RUNTIME (needs in-game test): block-deactivation delay — delaySupplier default is () -> 0 on the base, reassigned in the leaf ctor to MekanismConfig.general.blockDeactivationDelay. If the leaf reassignment is ever skipped (e.g. a subclass ctor path), machines would turn off instantly. Verify all leaf ctors reach the reassignment and that ACTIVE->inactive blockstate timing is unchanged in-game.
 - RUNTIME (needs in-game test): NBT round-trip on existing worlds — load a world saved before the split and confirm machines retain redstone control mode, active state, comparator level, custom names, stored energy/fluid/chemical/heat, and radiation scale. The byte-identity proof is structural; an in-game load/save/reload is the definitive check.
 - setRemoved super-chain direction: base.setRemoved is now a SUPER of CapabilityTileEntity.setRemoved. Confirmed correct order (CapabilityTileEntity invalidateAll -> super -> base components-loop+sound -> super -> TileEntityUpdateable), matching the old leaf chain. But re-verify in-game that breaking a machine still stops its sound and invalidates caps.
 - invalidateCapabilitiesFull / buildAndRegisterManagers / the 5 manager fields all live on CapabilityTileEntity (a SUBCLASS of the base). Any base method that needs them must reach them only via the 10 seam methods or inherited-from-subclass calls — which the base CANNOT make. Verified the only base methods touching caps are setFacing(notifyCaps) and configurationDataSet: setFacing(Direction,boolean) calls invalidateCapabilitiesFull -> KEEP setFacing(Direction,boolean) + setFacing(Direction) on the LEAF (or expose invalidateCapabilitiesFull as a protected no-op seam on base overridden by CapabilityTileEntity). RECOMMENDED: keep both setFacing overloads on the leaf (they are not annotation/NBT-critical and avoid a new seam). configurationDataSet is already leaf (WorldUtils + invalidateCapabilitiesFull).
 - getDirection reads/writes cachedDirection and is @ComputerMethod-pinned to the leaf, while setBlockState (base) also reads cachedDirection via invalidateDirectionCaches. cachedDirection promoted to protected resolves this, but confirm no ordering issue: setBlockState (base) calls invalidateDirectionCaches (base) which writes the protected field; getDirection (leaf) reads it. Fine.
 - Dropping final on 8 capability methods + soundEvent + the cap seam is an API-surface change; if any external addon relied on final/override-blocking it could now override. Acceptable for an internal split but note it.
 - MachineEnergyContainer bound widening (TileEntityMekanism -> TileEntityMekanismBase): verify NO neoforge subclass of MachineEnergyContainer re-narrows TILE to TileEntityMekanism expecting a leaf-only method, and no caller of input/internal/validateBlock breaks (widening is source-compatible, but a subclass that re-bounds must be checked during the atomic commit).
 - getSupportedUpgrade / AttributeUpgradeSupport: confirm AttributeUpgradeSupport is :common before placing getSupportedUpgrade on the base (it is called from base applyImplicitComponents recalculate loop). If neoforge, keep getSupportedUpgrade on leaf and have the base apply-hook call it via the leaf override path.
 - recalculateUpgrades moving to base depends on MachineEnergyContainer being :common (step 2) — ensure step 2 lands before/with step 4 in the SAME atomic commit, else recalculateUpgrades cannot compile in :common.

---

# Execution record (base-split LANDED)

Both loaders compile green; Fabric `:fabric:runServer` self-tests **11/11 PASS** (Config, Energy[api+lookup+blockEntity+machine], Heat, DataComponent, TextFoundation, Chemical, Fluid, Recipe, Power, AutoIo, Registration), clean `Done (…)!` boot, 0 FAIL. `MachineEnergyContainer` moved to `:common`; `TileEntityMekanismBase` (`:common`, NEW abstract) inserted between `TileEntityUpdateable` and `CapabilityTileEntity`; the leaf `TileEntityMekanism` shrank from ~1640 to ~720 lines (FQN preserved).

## Deviations from the written plan (all compile-gate-verified)
1. **getBlockHolder() is abstract on the base** (not a moved field). `blockProvider` + the whole ctor body stay on the leaf; the base ctor is just `(BlockEntityType,pos,state)`. This keeps `blockProvider` final on the leaf and avoids passing it through `CapabilityTileEntity`'s `ITileHolder` ctor. `ambientTemperature`/`soundEvent` drop `final` (assigned in the leaf ctor), `delaySupplier` defaults to `() -> 0` and is reassigned in the leaf ctor — as planned.
2. **applyHeatCapacitors/collectHeatCapacitors stay on the leaf** (the plan wrongly listed them as `:common`). They name NeoForge `AttachedHeat`/`HeatCapacitorData`, so — like the fluid pair on `CapabilityTileEntity` — they remain on the leaf and resolve via `ContainerType`'s `TileEntityMekanism::` method-refs.
3. **handleUpdateTag grand-super fix.** The leaf's `handleUpdateTag` historically called `super.loadAdditional` to reach the *lightweight* `TileEntityUpdateable.loadAdditional`. Now that the full `loadAdditional` lives on the base (an ancestor of the leaf), the leaf routes through a new `protected final TileEntityMekanismBase#loadAdditionalFromUpdateTag` (which does `super.loadAdditional`) so behavior is byte-identical.
4. **TileEntityTransmitter was NOT inert** (the plan's load-bearing assumption was wrong). `TileEntityTransmitter` is the *other* `CapabilityTileEntity` subclass, so re-parenting `CapabilityTileEntity` under the machine base forced the base's surface onto transmitters. Reconciled with behavior-preserving edits:
   - Added `getBlockHolder()` to `TileEntityTransmitter` (it already received `blockProvider`; now stored).
   - `onUpdateServer()` retyped `void -> boolean` across the transmitter tree (`TileEntityTransmitter` + `UniversalCable`/`MechanicalPipe`/`PressurizedTube`/`LogisticalTransporterBase`); the transmitter static `tickServer` ignores the return, so this is inert.
   - The transmitter private getters `getEnergyContainers`/`getFluidTanks`/`getChemicalTanks` were made `public` overrides of the base seam (identical bodies; capability exposure still flows through the per-transmitter `addCapabilityResolver`, not these methods).
   - The base now provides concrete (inert-for-transmitters) implementations of the leaf-only interface methods so non-machine subclasses stay concrete: `getDirection` (NORTH), `setFacing` (no-op), `getControlType`/`getCurrentRedstoneLevel` (read base fields), `getName` (block name), and the 4 `IConfigCardAccess` methods (loader-neutral sustained-data round trip). The machine leaf overrides every one with the real version (re-adding `@ComputerMethod` where applicable). `instanceof` audit: the only tile-level handler-interface checks are computer-integration (`MethodRestriction.ENERGY`, gated on `canHandleEnergy()` which is false for transmitters) — no behavioral impact.

## Still REQUIRED before merge (sub-stage 4 — in-game NeoForge, cannot run here)
Load a pre-split world with powered machines and confirm: stored energy round-trips; redstone control mode / active state / comparator level / custom names / stored fluid·chemical·heat / radiation scale all persist; adjacent machines/cables still push·pull energy (cap exposure identity); machine looping sounds + block-deactivation delay unchanged; **and** that transmitters (cables/pipes/tubes/transporters) still connect, transfer, and form networks normally (they were unexpectedly touched by this split).
