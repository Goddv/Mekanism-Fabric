

---

# Keystone: MekanismRecipeType base-split + IRecipeWorldAccess seam (Stage 4)

From the s-machines-recipetype-keystone workflow. VERDICTS: recipe-identity=sound-with-fixes (seam+base-split SOUND, verbatim world-access relocation); compile=defective + fabric=defective ON THE OVERREACH ONLY (Steps 4-full/5 hoisting the cache + lookup-handler layer carry net.neoforged via the ingredient API -> DEFERRED to a future ingredient-API stage). 

## EXECUTABLE SALVAGED SCOPE (this stage)
Seam (Steps 1-3) + IMekanismRecipeTypeProvider hoist (Step 4 PARTIAL, getRecipeType return re-typed to base) + WrappedSmelterRecipe/syntheticKey hoist (Step 7) + MekanismRecipeTypeBase (Step 6) + re-parent neoforge MekanismRecipeType (Step 8) + covariance verify (Step 9). DO NOT hoist the cache classes (Step 5) or the I*RecipeLookupHandler interfaces beyond IMekanismRecipeTypeProvider — they are net.neoforged-coupled (ingredient API).

## DEFERRED (the real next big wall): the FLUID/CHEMICAL INGREDIENT API (FluidStackIngredient=8 net.neoforged, ChemicalStackIngredient) must be loader-neutralized before the cache classes + lookup handlers + the prefab chain can reach :common. This is a MAJOR subsystem stage the roadmap under-weighted.

## Recipe-identity argument
NeoForge recipe-lookup behavior is preserved byte-for-byte because the concrete registered class MekanismRecipeType is NOT relocated and NOT renamed — it stays at mekanism.common.recipe.MekanismRecipeType on neoforge, keeps RECIPE_TYPES, all 27 public-static-final instances, register(), clearCache(), checkIncompleteRecipes(...), getRecipeFor(...), byKey(...), and remains the object instantiated by `new MekanismRecipeType<>(...)` and stored in the registry. Class identity of the registered RecipeType is therefore unchanged, which preserves the three identity-sensitive checks: recipeMap.byType(this) (the registry stores the same runtime class), this == SMELTING.get() in getRecipesUncached (SMELTING.get() returns the same neoforge instance, and the base's overridable hook getRegisteredSmeltingType() returns that same SMELTING static on neoforge), and castRecipe's o.getType() != this. \n\nThe caching SEMANTICS move verbatim into the base: getRecipes(RecipeMap) keeps the identical lazy-init (`if (cachedRecipes.isEmpty()) { ... = getRecipesUncached(recipeMap).stream().filter(r -> !r.value().isIncomplete()).toList(); }`), getRecipesUncached keeps recipeMap.byType(this) + the SMELTING vanilla-merge (WrappedSmelterRecipe.tryUnwrap + RegistryUtils.syntheticKey(id, \"mekanism_generated\") + new RecipeHolder<>), getInputCache()/clearCaches()/castRecipe()/toString()/getRegistryName()/getRecipeType() are copied unchanged. The only edited line of behavior is getRecipes(@Nullable Level): its branch ORDER and null handling are preserved exactly — ServerLevel fast-path FIRST (serverLevel.recipeAccess().recipeMap() stays in the base, since recipeAccess() returns vanilla RecipeManager and recipeMap() is callable on neoforge), then for the non-ServerLevel case delegate to IRecipeWorldAccess.INSTANCE.activeRecipeMap() which on NeoForge executes the SAME code the old method did (FMLEnvironment.getDist().isClient() ? MekanismClient.clientRecipes() : ServerLifecycleHooks.getCurrentServer()!=null ? currentServer.getRecipeManager().recipeMap() : null), then the identical `if (recipeMap == null) return Collections.emptyList(); return getRecipes(recipeMap);`. Because the NeoForge impl of activeRecipeMap() is a VERBATIM relocation of the exact branch bodies (same FMLEnvironment check, same MekanismClient.clientRecipes(), same ServerLifecycleHooks null-guard), the realized RecipeMap is identical, so byType(this) returns the same recipes, the incomplete-filter produces the same cachedRecipes, findFirst/contains/stream (all default methods on IMekanismRecipeTypeProvider, unchanged) iterate the same list, and getInputCache().initCacheIfNeeded(world) -> recipeType.getRecipes(world) resolves the same recipes. findFirstRecipe / input-cache resolution are thus provably unchanged.\n\nThe two static helpers getRecipeFor/byKey and checkMyIncompleteRecipes stay on neoforge with their bodies untouched (the ServerLevel cast + serverLevel.recipeAccess() vanilla calls; checkMyIncompleteRecipes still calls recipeManager.recipeMap() which is fine on neoforge). The dead private tryGetRegistryAccess() (0 callers, grep-confirmed) is deleted on neoforge — removing an unreferenced private method is behavior-identical. Net NeoForge source delta: MekanismRecipeType now `extends MekanismRecipeTypeBase` and its caching-method bodies are replaced by inheritance (same execution), the dead method is gone, and one branch routes through a seam whose neoforge impl is the original code. The user MUST in-game verify: (1) every machine still finds its recipe (enrichment/crushing/smelting/all chemical/fluid types), (2) smelting machine still accepts vanilla furnace recipes (the SMELTING merge), (3) world load does not log spurious 'Incomplete recipe detected' (checkIncompleteRecipes parity), (4) JEI/datapack recipe sync on join still shows all 27 types (Mekanism.java:197 getEntries() loop is untouched).

## Seam shape
IRecipeWorldAccess — a :common interface at mekanism.common.recipe.IRecipeWorldAccess following the ITileSyncService house pattern. The single NeoForge-patched operation is RecipeManager.recipeMap() (javap-confirmed absent on the Fabric RecipeManager), so the seam abstracts 'obtain the active RecipeMap', NOT 'obtain a RecipeManager'. Return type net.minecraft.world.item.crafting.RecipeMap is VANILLA (present in the Fabric merged jar) so NO net.neoforged appears in any signature.\n\nInterface (note: TWO methods — see the ServerLevel-fast-path risk: serverLevel.recipeAccess().recipeMap() is ALSO recipeMap() and so cannot stay inline in :common; route it through the seam too):\n  package mekanism.common.recipe;\n  import mekanism.api.MekanismAPIBase;\n  import net.minecraft.world.item.crafting.RecipeMap;\n  import net.minecraft.world.level.Level;\n  import org.jetbrains.annotations.ApiStatus.Internal;\n  import org.jetbrains.annotations.Nullable;\n  @Internal\n  public interface IRecipeWorldAccess {\n      IRecipeWorldAccess INSTANCE = MekanismAPIBase.getService(IRecipeWorldAccess.class);\n      /** Resolve the active RecipeMap for the given world (may be null), replicating MekanismRecipeType.getRecipes(Level)'s exact branch order: ServerLevel -> its recipeAccess().recipeMap(); else client -> client recipe map; else running server -> its recipe map; else null. */\n      @Nullable RecipeMap activeRecipeMap(@Nullable Level world);\n  }\n\nNeoForge impl — mekanism.common.recipe.NeoRecipeWorldAccess (neoforge/), a VERBATIM relocation of the entire original getRecipes(Level) acquisition block (lines 209-223), preserving branch order and null handling exactly:\n  public class NeoRecipeWorldAccess implements IRecipeWorldAccess {\n    @Nullable @Override public RecipeMap activeRecipeMap(@Nullable Level world) {\n      if (world instanceof ServerLevel serverLevel) { return serverLevel.recipeAccess().recipeMap(); }\n      if (FMLEnvironment.getDist().isClient()) { return MekanismClient.clientRecipes(); }\n      MinecraftServer s = ServerLifecycleHooks.getCurrentServer();\n      return s != null ? s.getRecipeManager().recipeMap() : null;\n    }\n  }\nImports ServerLevel, FMLEnvironment, MekanismClient (clientRecipes():RecipeMap), ServerLifecycleHooks, MinecraftServer — exactly the symbols removed from MekanismRecipeType.\n\nFabric impl — mekanism.fabric.recipe.FabricRecipeWorldAccess (fabric/): recipeMap() does not exist on Fabric's RecipeManager, so build one with the vanilla factory RecipeMap.create(server.getRecipeManager().getRecipes()) [getRecipes() is vanilla on the Fabric RecipeManager] using the ServerLevel when world is a ServerLevel (world.getServer()), else the running server obtained the Fabric way, else RecipeMap.EMPTY on the client for this stage. byType(this) over that map returns the same recipe set the direct serverLevel.recipeAccess().getRecipeFor path uses.\n\nService descriptors (getService throws if either loader is missing one — house invariant):\n  neoforge/src/main/resources/META-INF/services/mekanism.common.recipe.IRecipeWorldAccess -> mekanism.common.recipe.NeoRecipeWorldAccess\n  fabric/src/main/resources/META-INF/services/mekanism.common.recipe.IRecipeWorldAccess  -> mekanism.fabric.recipe.FabricRecipeWorldAccess\n\n(Refinement vs the one-method shape mentioned in steps 1-2/6: because the ServerLevel branch calls recipeMap() too, activeRecipeMap MUST take the @Nullable Level so the ServerLevel path also goes through the seam — otherwise the :common base cannot compile that branch. Adopt the @Nullable Level signature; the NeoForge impl above keeps the exact original branch order.)

## Steps


### Step 1: Create the IRecipeWorldAccess seam interface in :common
files: ['common/src/main/java/mekanism/common/recipe/IRecipeWorldAccess.java']
Add common/src/main/java/mekanism/common/recipe/IRecipeWorldAccess.java exactly as in seamShape: @Internal interface with `IRecipeWorldAccess INSTANCE = MekanismAPIBase.getService(IRecipeWorldAccess.class);` and one method `@Nullable RecipeMap activeRecipeMap();`. Only vanilla (RecipeMap) + mekanism.api.MekanismAPIBase imports — no net.neoforged. This compiles standalone before any consumer exists (the INSTANCE resolves at class-load, not compile). Mirrors ITileSyncService verbatim.

### Step 2: Add the NeoForge impl + service descriptor (verbatim relocation)
files: ['neoforge/src/main/java/mekanism/common/recipe/NeoRecipeWorldAccess.java', 'neoforge/src/main/resources/META-INF/services/mekanism.common.recipe.IRecipeWorldAccess']
Add neoforge/src/main/java/mekanism/common/recipe/NeoRecipeWorldAccess.java with activeRecipeMap() = the EXACT original branch bodies from MekanismRecipeType lines 213-219 (FMLEnvironment.getDist().isClient() ? MekanismClient.clientRecipes() : ServerLifecycleHooks.getCurrentServer()!=null ? currentServer.getRecipeManager().recipeMap() : null). Add neoforge/src/main/resources/META-INF/services/mekanism.common.recipe.IRecipeWorldAccess containing the single line `mekanism.common.recipe.NeoRecipeWorldAccess`. This keeps FMLEnvironment/MekanismClient/ServerLifecycleHooks usage byte-identical, just relocated.

### Step 3: Add the Fabric impl + service descriptor
files: ['fabric/src/main/java/mekanism/fabric/recipe/FabricRecipeWorldAccess.java', 'fabric/src/main/resources/META-INF/services/mekanism.common.recipe.IRecipeWorldAccess']
Add fabric/src/main/java/mekanism/fabric/recipe/FabricRecipeWorldAccess.java implementing activeRecipeMap() by building a RecipeMap via RecipeMap.create(server.getRecipeManager().getRecipes()) on the logical server (acquire the server ref the same way Fabric does elsewhere; getRecipes() is vanilla on the Fabric RecipeManager) and returning RecipeMap.EMPTY on the client for this stage (Fabric machines currently bypass the cache via serverLevel.recipeAccess().getRecipeFor, so an EMPTY client map is behavior-neutral for THIS stage). Add fabric/src/main/resources/META-INF/services/mekanism.common.recipe.IRecipeWorldAccess containing `mekanism.fabric.recipe.FabricRecipeWorldAccess`. This is the one non-mechanical piece — see risks; the goal is byType(this) returning the same recipe set the direct lookup uses.

### Step 4: Hoist the lookup CONTRACT interfaces to :common
files: ['common/src/main/java/mekanism/common/recipe/IMekanismRecipeTypeProvider.java', 'common/src/main/java/mekanism/common/recipe/lookup/IRecipeLookupHandler.java', 'common/src/main/java/mekanism/common/recipe/lookup/ISingleRecipeLookupHandler.java', 'common/src/main/java/mekanism/common/recipe/lookup/IDoubleRecipeLookupHandler.java', 'common/src/main/java/mekanism/common/recipe/lookup/ITripleRecipeLookupHandler.java', 'common/src/main/java/mekanism/common/recipe/lookup/IEitherSideRecipeLookupHandler.java']
Move (same FQN) IMekanismRecipeTypeProvider.java and IRecipeLookupHandler.java + I{Single,Double,Triple,EitherSide}RecipeLookupHandler.java from neoforge/src/main/java/mekanism/common/recipe/lookup/ to common/src/main/java/mekanism/common/recipe/lookup/. IMekanismRecipeTypeProvider imports only vanilla + RecipeMap + IInputRecipeCache (already common) + MekanismRecipe (api/common) — but it names MekanismRecipeType<...> in getRecipeType()'s return type. Re-type that return to MekanismRecipeTypeBase<...> (created in step 6) so the interface lives in :common without seeing the neoforge subclass. IRecipeLookupHandler already uses only the provider (grep-confirmed: 0 concrete-class refs). Verify each I*RecipeLookupHandler has no net.neoforged import (grep-confirmed the lookup tree is fml/neoforged-free).

### Step 5: Hoist the cache classes + monitors to :common, re-typing the recipeType handle to the base
files: ['common/src/main/java/mekanism/common/recipe/lookup/cache/AbstractInputRecipeCache.java', 'common/src/main/java/mekanism/common/recipe/lookup/cache/SingleInputRecipeCache.java', 'common/src/main/java/mekanism/common/recipe/lookup/cache/DoubleInputRecipeCache.java', 'common/src/main/java/mekanism/common/recipe/lookup/cache/TripleInputRecipeCache.java', 'common/src/main/java/mekanism/common/recipe/lookup/cache/EitherSideInputRecipeCache.java', 'common/src/main/java/mekanism/common/recipe/lookup/cache/RotaryInputRecipeCache.java', 'common/src/main/java/mekanism/common/recipe/lookup/cache/InputRecipeCache.java', 'common/src/main/java/mekanism/common/recipe/lookup/cache/type/ChemicalInputCache.java', 'common/src/main/java/mekanism/common/recipe/lookup/cache/type/ComponentSensitiveInputCache.java', 'common/src/main/java/mekanism/common/recipe/lookup/cache/type/FluidInputCache.java', 'common/src/main/java/mekanism/common/recipe/lookup/cache/type/ItemInputCache.java', 'common/src/main/java/mekanism/common/recipe/lookup/monitor/RecipeCacheLookupMonitor.java', 'common/src/main/java/mekanism/common/recipe/lookup/monitor/FactoryRecipeCacheLookupMonitor.java', 'common/src/main/java/mekanism/common/recipe/lookup/monitor/NucleosynthesizerRecipeCacheLookupMonitor.java']
Move recipe/lookup/cache/{AbstractInputRecipeCache,SingleInputRecipeCache,DoubleInputRecipeCache,TripleInputRecipeCache,EitherSideInputRecipeCache,RotaryInputRecipeCache,InputRecipeCache}.java + recipe/lookup/cache/type/{ChemicalInputCache,ComponentSensitiveInputCache,FluidInputCache,ItemInputCache}.java + recipe/lookup/monitor/{RecipeCacheLookupMonitor,FactoryRecipeCacheLookupMonitor,NucleosynthesizerRecipeCacheLookupMonitor}.java from neoforge to common (same FQNs). In every cache file, change the ctor param/field type `MekanismRecipeType<?, RECIPE, ?>` -> `MekanismRecipeTypeBase<?, RECIPE, ?>` and update the import (only .getRecipes(world) is called on it — grep-confirmed — which lives on the base). IInputRecipeCache/IInputCache/BaseInputCache are already in :common. Verify no net.neoforged imports remain in any moved file. Note: ChemicalInputCache wasn't in the earlier neoforged-import grep hit-list so confirm it too is clean before moving.

### Step 6: Create MekanismRecipeTypeBase in :common (the caching core, via the seam)
files: ['common/src/main/java/mekanism/common/recipe/MekanismRecipeTypeBase.java']
Add common/src/main/java/mekanism/common/recipe/MekanismRecipeTypeBase.java: `public abstract class MekanismRecipeTypeBase<VANILLA_INPUT extends RecipeInput, RECIPE extends MekanismRecipe<VANILLA_INPUT>, INPUT_CACHE extends IInputRecipeCache> implements RecipeType<RECIPE>, IMekanismRecipeTypeProvider<VANILLA_INPUT, RECIPE, INPUT_CACHE>`. Move into it VERBATIM: fields cachedRecipes/registryName/inputCache; a protected ctor `MekanismRecipeTypeBase(Identifier name, Function<MekanismRecipeTypeBase<...>, INPUT_CACHE> inputCacheCreator)` (param typed to the base so :common cache ctors accept it); toString/getRegistryName/getRecipeType (getRecipeType returns this, typed as MekanismRecipeTypeBase<...>); clearCaches/getInputCache; getRecipes(@Nullable Level) with the ServerLevel fast-path inline (serverLevel.recipeAccess().recipeMap()) and the non-ServerLevel branch -> `RecipeMap recipeMap = IRecipeWorldAccess.INSTANCE.activeRecipeMap();` then identical null-guard + delegate; getRecipes(RecipeMap) (lazy-init+incomplete-filter verbatim); getRecipesUncached(RecipeMap) verbatim EXCEPT replace `this == SMELTING.get()` with `this == getRegisteredSmeltingType()` and add `protected abstract MekanismRecipeTypeBase<?,?,?> getRegisteredSmeltingType();` (overridable hook so the base never names the neoforge SMELTING static); castRecipe verbatim; checkMyIncompleteRecipes(RecipeMap) — change signature from RecipeManager to RecipeMap so it is loader-clean (its sole call chain holds a RecipeManager and can call .recipeMap() at the neoforge static). NOTE the inline ServerLevel fast-path: serverLevel.recipeAccess().recipeMap() — recipeMap() is neoforge-only, so for :common this path must ALSO route through the seam OR through a covariant; SAFER: route the ENTIRE acquisition (including ServerLevel) through IRecipeWorldAccess by adding a second seam method activeRecipeMap(@Nullable Level) — see risks. Carry WrappedSmelterRecipe + RegistryUtils.syntheticKey to :common (step 7) so getRecipesUncached compiles here.

### Step 7: Hoist WrappedSmelterRecipe + RegistryUtils.syntheticKey to :common (SMELTING-merge deps)
files: ['common/src/main/java/mekanism/common/recipe/WrappedSmelterRecipe.java', 'common/src/main/java/mekanism/common/util/RegistryUtils.java']
getRecipesUncached's SMELTING branch needs WrappedSmelterRecipe.tryUnwrap and RegistryUtils.syntheticKey(id, "mekanism_generated"). WrappedSmelterRecipe has ZERO neoforge imports (grep-confirmed) -> move neoforge/.../recipe/WrappedSmelterRecipe.java to common/ same FQN. RegistryUtils has 13 imports total; verify syntheticKey's transitive deps are loader-clean, then EITHER move RegistryUtils to :common (if fully clean) OR extract just syntheticKey into a :common helper (e.g. an existing common util) the base calls. The synthetic ResourceKey id string 'mekanism_generated' must be byte-identical (save/sync-adjacent). Confirm BuiltInRegistries/ResourceKey usage in syntheticKey is vanilla.

### Step 8: Re-parent the neoforge MekanismRecipeType onto the base; delete dead code; keep ALL registration
files: ['neoforge/src/main/java/mekanism/common/recipe/MekanismRecipeType.java']
Edit neoforge/.../recipe/MekanismRecipeType.java: change header to `public class MekanismRecipeType<...> extends MekanismRecipeTypeBase<...>` (drop the now-inherited `implements RecipeType, IMekanismRecipeTypeProvider`). Remove the caching members now on the base (cachedRecipes/registryName/inputCache fields, toString/getRegistryName/getRecipeType/clearCaches/getInputCache/getRecipes(Level)/getRecipes(RecipeMap)/getRecipesUncached/castRecipe). Change the private ctor to call `super(name, inputCacheCreator::apply)` (adapt the Function type from MekanismRecipeType<...> to the base's MekanismRecipeTypeBase<...> param — the register() lambdas pass `recipeType -> new SingleItem<>(recipeType,...)` which now bind recipeType as the base type; since cache ctors were re-typed to the base in step 5 this type-checks). Implement the abstract hook: `@Override protected MekanismRecipeTypeBase<?,?,?> getRegisteredSmeltingType() { return SMELTING.get(); }`. DELETE the dead private tryGetRegistryAccess() (0 callers, grep-confirmed — behavior-identical). KEEP byte-identical: RECIPE_TYPES, all 27 public-static-final statics, register(), clearCache(), checkIncompleteRecipes(MinecraftServer)+(RecipeManager,RegistryAccess), getRecipeFor(...), byKey(...). Update checkIncompleteRecipes' inner loop to call the base's checkMyIncompleteRecipes(recipeManager.recipeMap()) [pass the RecipeMap, materialized on neoforge via recipeMap()]. The class name, FQN, 27 static FQNs, and 6 helper FQNs are unchanged -> all 91 importers + Mekanism.java:197/244 untouched.

### Step 9: Fix neoforge registration generics that name MekanismRecipeType where the base is now expected
files: ['neoforge/src/main/java/mekanism/common/registration/impl/RecipeTypeRegistryObject.java', 'neoforge/src/main/java/mekanism/common/registration/impl/RecipeTypeDeferredRegister.java']
RecipeTypeDeferredRegister.registerMek takes `Function<Identifier, ? extends MekanismRecipeType<...>>` and RecipeTypeRegistryObject extends MekanismDeferredHolder<RecipeType<?>, MekanismRecipeType<...>> implements IMekanismRecipeTypeProvider with getRecipeType():MekanismRecipeType<...>. These stay neoforge and continue naming the concrete neoforge MekanismRecipeType (which still exists at the same FQN) — so NO change is needed IF getRecipeType()'s covariant return (MekanismRecipeType extends the base's MekanismRecipeTypeBase return) is accepted. Verify RecipeTypeRegistryObject.getRecipeType() override return type (MekanismRecipeType<...>) is covariant-compatible with the hoisted IMekanismRecipeTypeProvider.getRecipeType() return (now MekanismRecipeTypeBase<...>): MekanismRecipeType IS-A MekanismRecipeTypeBase so the covariant override is legal. No edit expected; this step is a verification gate. If the compiler rejects the covariance, widen RecipeTypeRegistryObject's value()/getRecipeType() as minimally as possible without touching the 91 importers.

### Step 10: Compile-gate both loaders + run the Fabric self-tests
files: []
Build neoforge (must be byte-identical-behavior: it compiles, the 91 importers resolve, registration unchanged). Build fabric (the :common base + cache + lookup interfaces now compile in :common; FabricRecipeWorldAccess satisfies the service). Run Fabric runServer self-tests (currently 11/11 incl. the dirt->diamond recipe self-test) — they must stay green, proving the :common caching core resolves recipes through the Fabric seam consistently with the direct serverLevel.recipeAccess().getRecipeFor path. The NeoForge in-game checks listed in recipeIdentityArgument are the human guardrail.