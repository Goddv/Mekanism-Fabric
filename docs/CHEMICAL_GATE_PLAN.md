# Chemical/Fluid Master-Gate Hoist Plan (from chemical-gate-plan workflow)

## Closure summary
VERIFIED the layer maps against source and found one material correction plus a clear smallest first slice.

CORRECTION TO LAYER MAP (handlers/tanks "cleanly hoistable" claim is WRONG): The 7 files in the "chemical-handlers-tanks" layer (IChemicalHandler, ISidedChemicalHandler, IMekanismChemicalHandler, IChemicalTank, BasicChemicalTank, ChemicalUtils, ChemicalAttributeValidator) ALL hard-reference ChemicalStack in method signatures and via ChemicalStack.EMPTY / ChemicalStack.CODEC / ChemicalStack.isSameChemical; IChemicalTank+BasicChemicalTank also use Holder<Chemical>; ChemicalAttributeValidator imports mekanism.api.chemical.Chemical, mekanism.api.chemical.ChemicalStack, and mekanism.api.datamaps.chemical.attribute.IChemicalAttribute directly (lines 4-6). Since Chemical/ChemicalStack stay in :neoforge (NeoForge DataMapType + registry + radiation coupling, confirmed), hoisting these would create an illegal :common -> :neoforge dependency. They are NOT a valid first slice; they must wait for the Chemical/ChemicalStack inheritance-split.

THE SMALLEST TRACTABLE FIRST SLICE = hoist the two pure leaf prerequisites of the api.text foundation: mekanism.api.IIncrementalEnum and mekanism.api.SupportsColorMap. These are the deepest foundational prereqs for EnumColor (which gates ChemicalStack.appendHoverText, which gates the chemical core). Verified pure:
- SupportsColorMap.java imports ONLY net.minecraft.util.ARGB (vanilla) -> zero changes needed on git mv.
- IIncrementalEnum.java imports mekanism.api.annotations.NothingNullByDefault (already in :common) and mekanism.api.functions.ConstantPredicates (line 5, the :neoforge subclass). The ONLY edit needed: retarget that import to mekanism.api.functions.ConstantPredicatesBase (already in :common; static alwaysTrue() lives there at lines 68-70). This mirrors the proven ConstantPredicates/ConstantPredicatesBase inheritance-split discipline exactly.

Why this is a genuine step toward the master gate: EnumColor (the next domino) implements IIncrementalEnum + SupportsColorMap and cannot move to :common until both are there. EnumColor in turn is referenced by ChemicalStack.appendHoverText, the text bridge gating the chemical core. So this slice unblocks the text-foundation chain that the chemical-core-types layer depends on, with the smallest possible blast radius (two leaf interfaces, one import edit, zero behavior change).

Why EnumColor itself is NOT the first slice: it depends on APILang, ILangEntry, and TextComponentUtil.build() (EnumColor.java line 136), all NeoForge-located; TextComponentUtil is NeoForge-coupled (imports FMLEnvironment + FluidStack + FluidResource/ItemResource) and needs a split first. ITooltipHelper is close (getService is in MekanismAPIBase in :common) but its line-3 import of mekanism.api.MekanismAPI must be retargeted to MekanismAPIBase, and it is NOT a prereq for the chemical core types, so it is deferred to the next slice.

Call-site safety verified: IIncrementalEnum/SupportsColorMap have ~25 importers across :neoforge (e.g. EnumColor, SecurityMode, BaseTier, DataType, many GUI/item/tile classes) and the package path (mekanism.api.*) is unchanged by git mv, so every existing import keeps resolving once :common is on both loaders' classpaths (same proven mechanism as the energy/heat hoist). Nothing in :common imports them yet, so no forward dependency risk.

Self-test mechanism verified: MekanismFabric.onInitialize() (fabric/src/main/java/mekanism/fabric/MekanismFabric.java lines 55-59) runs FabricEnergySelfTest/FabricHeatSelfTest/FabricDataComponentSelfTest/FabricRegistrationSelfTest under isDevelopmentEnvironment(). These leaves are pure-logic (no capability/BlockApiLookup), so the validation follows the FabricRegistrationSelfTest pure-logic style (LogUtils logger, [Mekanism/Fabric][...-selftest] TAG, RESULT: PASS/FAIL), NOT the block-entity-placement style of FabricHeatSelfTest.

## Ordered hoist sequence
1. Slice 1 (FIRST, this slice): git mv neoforge/src/api/java/mekanism/api/IIncrementalEnum.java and neoforge/src/api/java/mekanism/api/SupportsColorMap.java to common/src/main/java/mekanism/api/. Edit IIncrementalEnum line 5 import from mekanism.api.functions.ConstantPredicates to mekanism.api.functions.ConstantPredicatesBase. Add a Fabric pure-logic self-test.
   - The only two genuinely pure leaves in the whole chemical/text chain. Foundational prereqs for EnumColor. One-line import retarget mirrors the proven ConstantPredicatesBase split. Zero :common->:neoforge leakage, zero behavior change, ~25 unchanged call sites.
2. Slice 2: Retarget ITooltipHelper line-3 import from mekanism.api.MekanismAPI to mekanism.api.MekanismAPIBase, then git mv ITooltipHelper.java to common. Extract IHasTextComponent's nested NeoForge-coupled IHasEnumNameTextComponent into a new top-level :neoforge interface, then git mv the now-pure IHasTextComponent base to common.
   - ITooltipHelper's getService already lives in MekanismAPIBase (:common); only the import points at the subclass. IHasTextComponent base is loader-neutral once its TranslatableEnum-coupled nested type is extracted (same nested-interface-extraction trick used for IHasTranslationKey).
3. Slice 3: Split TextComponentUtil -> create common TextComponentUtilBase with vanilla-only build()/smartTranslate() (drop FMLEnvironment, FluidStack, FluidStackTemplate, FluidResource, ItemResource branches + imports); keep a :neoforge TextComponentUtil subclass overriding build()/smartTranslate() with the fluid/resource branches and logLiteralItemUsage.
   - TextComponentUtil is the load-bearing NeoForge coupling (imports FMLEnvironment + fluid/transfer types). The inheritance-split keeps NeoForge fluid rendering in :neoforge while exposing the vanilla core to :common, unblocking ILangEntry and EnumColor.
4. Slice 4: git mv ILangEntry, then APILang, then EnumColor to common (in that order). ILangEntry's default methods bind to TextComponentUtilBase; APILang only needs MekanismAPIBase.MEKANISM_MODID (already in :common); EnumColor now resolves IIncrementalEnum, SupportsColorMap, APILang, ILangEntry, TextComponentUtilBase all in :common.
   - EnumColor (EnumColor.java line 136 calls TextComponentUtil.build; line 145 calls langEntry.translate) is unblocked only after its text deps are in :common. Completing EnumColor is the gate that lets ChemicalStack.appendHoverText be hoisted later.
5. Slice 5: Move CHEMICAL_REGISTRY_NAME + EMPTY_CHEMICAL_KEY constants from MekanismAPI down to MekanismAPIBase (registryKey helper already there); hoist SerializerHelper to common minus LENIENT_OPTIONAL_FLUID_CODEC (keep that in a :neoforge holder); hoist MekanismAPITags unchanged; create an Architectury ChemicalDeferredRegister in :common mirroring ParticleTypeDeferredRegister.
   - These are the registry/serializer prerequisites for the Chemical core that do NOT depend on Chemical itself. Verified: SerializerHelper's only NeoForge dep is the 3-line FluidStack codec; MekanismAPITags only needs the registry name constant relocated to MekanismAPIBase.
6. Slice 6: Hoist ChemicalBuilder (pure structure, zero registry/loader deps) and ChemicalInstance (minimal interface over Chemical codec statics) to common.
   - Layer map confirms these two core types are pure once the registry-name constants exist in :common; they are the leaf entry into the chemical-core-types layer.
7. Slice 7: Inheritance-split Chemical -> ChemicalBase in :common (codec/holder core referencing the registry-access abstraction) keeping a :neoforge Chemical subclass for DataMapType (updateFromDataMap line ~208) and IRadiationManager (hasAttributesWithValidation line ~144). Then hoist ChemicalStack with text bridge now resolvable.
   - Chemical/ChemicalStack are the master gate; they are hoistable only after the text foundation (slices 1-4) and registry constants (slice 5) land, using the same MekanismAPI/MekanismAPIBase inheritance-split for the datamap/radiation coupling.
8. Slice 8: Hoist the handlers+tanks layer (IChemicalHandler, ISidedChemicalHandler, IMekanismChemicalHandler, IChemicalTank, BasicChemicalTank, ChemicalUtils, ChemicalAttributeValidator) to common and add the Fabric MekanismFabricChemical BlockApiLookup mirror + a capability self-test.
   - CORRECTED ORDERING: these are leaf-shaped but hard-depend on ChemicalStack/Chemical/IChemicalAttribute, so they can only move AFTER slice 7, not first as the layer map asserted. Once Chemical/ChemicalStack are in :common this layer follows the proven energy/heat capability-mirror pattern.
9. Slice 9: Hoist the attribute subsystem (IChemicalAttribute, ChemicalFuel, ChemicalRadioactivity, IChemicalCoolant, CooledCoolant, HeatedCoolant) once Chemical, the text helpers, and IMekanismDataMapTypes abstraction are in :common; keep IRadiationManager runtime coupling as-is.
   - Attributes are circularly bound to Chemical.getAttributes() and the data-map infra and chain through APILang/ITooltipHelper tooltips; they are the last domino, deferred until everything they touch is loader-neutral.

## Per-layer coupling maps
### api-text-foundation
The text foundation layer is split between loader-neutral (EnumColor, APILang, ITooltipHelper, IHasTextComponent base) and NeoForge-coupled (TextComponentUtil's fluid branches, IHasTextComponent.IHasEnumNameTextComponent, ILangEntry's default methods that call TextComponentUtil). EnumColor is fully hoistable (only vanilla + pure-API deps); APILang is hoistable (MekanismAPIBase.MEKANISM_MODID is already in :common); ITooltipHelper is hoistable (service locator pattern, implementation registered loader-specifically). IHasTextComponent requires extracting its nested IHasEnumNameTextComponent to a NeoForge-only interface (leaving the base pure). TextComponentUtil must split: core vanilla-compatible build/smartTranslate logic goes to :common (TextComponentUtilBase), while NeoForge-specific case branches (FluidStack, FluidResource, ItemResource) stay in NeoForge subclass. ILangEntry depends on TextComponentUtil, so must wait for split. Blocker chain: IIncrementalEnum + SupportsColorMap (prereqs for EnumColor) are still in :neoforge but loader-neutral, must be hoisted first.
Coupling blockers: TextComponentUtil imports net.neoforged.fml.loading.FMLEnvironment (line 25); TextComponentUtil imports net.neoforged.neoforge.fluids.FluidStack/FluidStackTemplate (lines 26-27); TextComponentUtil imports net.neoforged.neoforge.transfer.{FluidResource,ItemResource} (lines 28-29); IHasTextComponent.IHasEnumNameTextComponent extends net.neoforged.neoforge.common.TranslatableEnum (line 24 in IHasTextComponent); ILangEntry.translateColored(EnumColor, ...) and translate() call TextComponentUtil.smartTranslate/build which must be split; TextComponentUtil.build(Object...) and smartTranslate(String, Object...) have heavy NeoForge case branches for FluidStack/Resource/ItemResource
Hoistable now: ITooltipHelper (pure service locator interface, no loader-specific code, only MekanismAPI.getService() which works in :common); APILang (only depends on MekanismAPI.MEKANISM_MODID which is in MekanismAPIBase already in :common, Util.makeDescriptionId is vanilla)
Deferred: EnumColor (hoistable after IIncrementalEnum + SupportsColorMap moved to :common, currently in :neoforge only); IHasTextComponent base interface (hoistable once IHasEnumNameTextComponent is extracted to separate :neoforge file); ILangEntry (depends on TextComponentUtil split), TextComponentUtil.build()/smartTranslate() base (must split from NeoForge branches first), TextComponentUtil NeoForge-specific version (must stay in :neoforge with fluid/item resource cases)

### chemical-core-types
The five core chemical types exhibit MIXED hoistability. Chemical, ChemicalStack, ChemicalBuilder, and ChemicalInstance are loader-neutral in structure but depend on the CHEMICAL_REGISTRY (currently a NeoForge RegistryBuilder in :neoforge MekanismAPI) and text API classes that are partially NeoForge-coupled (EnumColor, APILang, IHasTextComponent all reside in :neoforge). ChemicalResource also depends on RegisteredResource<Chemical> from NeoForge transfer API. The data-map attribute system (IChemicalAttribute and its impls like ChemicalRadioactivity) is service-driven via IMekanismDataMapTypes.INSTANCE but uses IRadiationManager and text components. BLOCKING BLOCKER: the CHEMICAL_REGISTRY itself is declared in :neoforge MekanismAPI (lines 81-84) as a RegistryBuilder with NeoForge-specific RegistryBuilder/DefaultedRegistry types. Inheritance-split strategy needed: extract Chemical/ChemicalStack/ChemicalBuilder/ChemicalInstance to :common with reduced deps, keep NeoForge subclass or redirect registry access. Text API must hoist IHasTextComponent before chemical types can be split.
Coupling blockers: net.neoforged.neoforge.registries.RegistryBuilder and NeoForge DefaultedRegistry in MekanismAPI.CHEMICAL_REGISTRY (lines 81-84 of MekanismAPI.java) — registry creation itself is NeoForge-bound; net.neoforged.neoforge.registries.DeferredHolder in Chemical.STREAM_CODEC (line 51) — used by Chemical.CODEC via ByteBufCodecs.holderRegistry(MekanismAPI.CHEMICAL_REGISTRY_NAME); mekanism.api.text.EnumColor, APILang, TextComponentUtil (NeoForge-located at neoforge/src/api/java/mekanism/api/text/) used in ChemicalStack.appendHoverText() (lines 402-407); net.neoforged.neoforge.transfer.resource.RegisteredResource<Chemical> in ChemicalResource (line 14); mekanism.api.datamaps.IMekanismDataMapTypes and NeoForge DataMapType<Chemical, ?> in Chemical.updateFromDataMap() (line 208); mekanism.api.radiation.IRadiationManager.INSTANCE.isRadiationEnabled() in Chemical.hasAttributesWithValidation() (line 144); mekanism.api.text.IHasTextComponent (currently NeoForge-located) required by Chemical (line 14 Chemical.java, line 38 declares implements IHasTextComponent)
Hoistable now: ChemicalBuilder.java — pure structure: final Identifier texture, int tint field, getters, and static builders. Zero loader deps. Zero registry deps. (neoforge/src/api/java/mekanism/api/chemical/ChemicalBuilder.java lines 1-104); ChemicalInstance.java — minimal interface declaring amount() method + two static codec/streamcodec fields. Depends only on Chemical.CODEC/STREAM_CODEC statics (which will be hoisted) and MekanismAPI.EMPTY_CHEMICAL_KEY constant. (lines 1-32)
Deferred: Chemical.java — blocked by IHasTextComponent location (NeoForge), Chemical.CODEC/STREAM_CODEC static initialization (depends on MekanismAPI.CHEMICAL_REGISTRY which is RegistryBuilder-created in :neoforge), and IRadiationManager.INSTANCE service lookup. Once text API is hoisted and registry access is refactored, can hoist with registry-accessor strategy.; ChemicalStack.java — blocked by SerializerHelper.POSITIVE_LONG_CODEC (can be hoisted separately), MekanismAPI/MekanismAPITags (tag system tied to registries), text APIs (APILang, EnumColor, IHasTextComponent), and Chemical.STREAM_CODEC. Blocked until Chemical is hoisted.; ChemicalResource.java — blocked by RegisteredResource<Chemical> (NeoForge transfer API), and ChemicalStack hoisting. Blocked until NeoForge transfer API is mirrored.; Attribute classes (ChemicalRadioactivity, ChemicalFuel, IChemicalCoolant, etc.) — blocked by IRadiationManager.INSTANCE service, APILang/EnumColor/ITooltipHelper (text system). Can be partially hoisted after text API split.

### chemical-attributes
The chemical attribute system is DEFERRED pending Chemical/ChemicalStack hoisting. It comprises 7 files (ChemicalAttributeValidator + 6 attribute type classes) with CRITICAL NeoForge+text coupling: (1) attributes reference mekanism.api.datamaps.chemical.attribute.IChemicalAttribute and DataMapType (NeoForge registry), (2) tooltip rendering chains through mekanism.api.text.* (APILang/EnumColor/ITooltipHelper—all in :neoforge), (3) Chemical.updateFromDataMap() uses NeoForge DataMapType API (net.neoforged.neoforge.registries.datamaps), (4) IRadiationManager is queried at runtime for validation. CANNOT hoist attributes independently; Chemical and ChemicalStack must be hoisted first, along with a :common abstraction for IMekanismDataMapTypes and the tooltip text helpers. The attribute files themselves are LEAF (no internal coupling, pure data records), but their load-bearing contract with Chemical.getAttributes() and the data-map infrastructure blocks them.
Coupling blockers: Chemical.java line 86-87: private List&lt;IChemicalAttribute&gt; attributes — storage contract; Chemical.java line 208-218: trackAttribute(Holder, DataMapType) uses net.neoforged.neoforge.registries.datamaps.DataMapType; Chemical.java line 12: import mekanism.api.datamaps.chemical.attribute.IChemicalAttribute; ChemicalRadioactivity.java line 35-40: RADIATION_CODEC validates against IRadiationManager.INSTANCE.baselineRadiation() (runtime check at load); ChemicalRadioactivity.java line 63: collectTooltips() → APILang.CHEMICAL_ATTRIBUTE_RADIATION (not hoisted); ChemicalFuel.java line 60-66: collectTooltips() → APILang.CHEMICAL_ATTRIBUTE_FUEL_* (not hoisted); IChemicalCoolant.java line 47-51: collectTooltips() → APILang.CHEMICAL_ATTRIBUTE_COOLANT_* + ITooltipHelper.INSTANCE (not hoisted); HeatedCoolant.java line 79-81: collectTooltips() → APILang.CHEMICAL_ATTRIBUTE_COOLANT_TEMPERATURE (not hoisted); BasicChemicalTank.java line 11: import mekanism.api.chemical.attribute.ChemicalAttributeValidator; IChemicalTank.java line 8: import mekanism.api.chemical.attribute.ChemicalAttributeValidator; ChemicalAttributeValidator.java line 4,5,6: imports Chemical, ChemicalStack, IChemicalAttribute — circular dependency with Chemical
Hoistable now: 
Deferred: mekanism/api/chemical/attribute/ChemicalAttributeValidator.java — imports Chemical and ChemicalStack (not yet hoisted); deferred until Chemical/ChemicalStack move to :common; mekanism/api/datamaps/chemical/attribute/IChemicalAttribute.java — marker interface, but used by Chemical.getAttributes() and data-map infrastructure (deferred with Chemical); mekanism/api/datamaps/chemical/attribute/ChemicalFuel.java — pure data record, but collectTooltips() requires APILang (not hoisted), codec uses SerializerHelper (in :neoforge); deferred with attributes system; mekanism/api/datamaps/chemical/attribute/ChemicalRadioactivity.java — pure data record, but collectTooltips() requires APILang + ITooltipHelper (not hoisted), codec validates against IRadiationManager.INSTANCE (runtime behavior); deferred with attributes system; mekanism/api/datamaps/chemical/attribute/IChemicalCoolant.java — sealed interface with collectTooltips() → APILang + ITooltipHelper, createBaseCodec() references ChemicalInstance.CHEMICAL_HOLDER_CODEC (not deferred separately); deferred with attributes system; mekanism/api/datamaps/chemical/attribute/CooledCoolant.java — record implementing IChemicalCoolant; deferred with attributes system; mekanism/api/datamaps/chemical/attribute/HeatedCoolant.java — record implementing IChemicalCoolant with temperature field; collectTooltips() → APILang.CHEMICAL_ATTRIBUTE_COOLANT_TEMPERATURE; deferred with attributes system

### chemical-handlers-tanks
The chemical handlers + tanks layer (analog of energy/heat) is **cleanly hoistable to :common** as leaf interfaces and implementations. All 6 core files (IChemicalHandler, ISidedChemicalHandler, IMekanismChemicalHandler, IChemicalTank, BasicChemicalTank, ChemicalUtils) depend ONLY on: (1) loader-neutral types already in :common (Action, AutomationType, IContentsListener, SerializationConstants, IValueIOSerializable, ChemicalAttributeValidator, ConstantPredicates, container types); (2) vanilla Minecraft API; (3) ChemicalStack + Chemical (which remain in :neoforge as they have NeoForge DataMapType coupling). The only NeoForge imports in these files are javadoc references to FluidStack/IFluidHandler for doc clarity. No blocking dependency prevents hoisting; the Fabric BlockApiLookup mirror (MekanismFabricChemical parallel to MekanismFabricEnergy/Heat) follows the proven pattern with typed Direction context.
Coupling blockers: Chemical.java (net.neoforged.neoforge.registries.datamaps.DataMapType import) — must stay in :neoforge; ChemicalStack imports Chemical + MekanismAPI registry; ChemicalStack.java (imports MekanismAPITags, SerializerHelper, APILang, EnumColor, IHasTextComponent, TextComponentUtil from mekanism.api.text; plus MekanismAPI registry holder) — stays in :neoforge as text rendering bridge; MekanismAPI.CHEMICAL_REGISTRY (registry holder, only in :neoforge subclass MekanismAPI) — hoisted interfaces reference it via Holder<Chemical>, not direct static access
Hoistable now: IChemicalHandler.java (leaf interface, no NeoForge deps except javadoc); ISidedChemicalHandler.java (leaf interface extending IChemicalHandler, adds Direction context); IMekanismChemicalHandler.java (leaf interface extending ISidedChemicalHandler + IContentsListener, adds List<IChemicalTank> + AutomationType.handler() dispatch); IChemicalTank.java (leaf interface extending IValueIOSerializable + IContentsListener, serialize/deserialize methods are loader-neutral); BasicChemicalTank.java (concrete implementation of IChemicalTank + IChemicalHandler, implements insert/extract with rate-limiting; no NeoForge deps); ChemicalUtils.java (utility methods for multi-tank distribution; uses generic containers + Direction; no NeoForge deps); ChemicalAttributeValidator.java (interface + nested SimpleAttributeValidator; validates attributes; no NeoForge deps)
Deferred: Chemical.java — has DataMapType import; extract to ChemicalBase in :common for the codec/holder parts if needed, leave Chemical in :neoforge; ChemicalStack.java — bridges to text rendering (EnumColor, APILang, TextComponentUtil); keep in :neoforge as wrapper over ChemicalInstance (vanilla interface)

### serializer-tags-registry
SerializerHelper can be hoisted as-is except the LENIENT_OPTIONAL_FLUID_CODEC (1 codec, 3 lines), which stays in :neoforge; MekanismAPITags is fully loader-neutral and safe to hoist; the CHEMICAL_REGISTRY holder requires a :common Architectury-backed ChemicalDeferredRegister mirroring ParticleTypeDeferredRegister, with registry keys/names moved to MekanismAPIBase. The Chemical class itself remains in :neoforge (datamaps + radiation manager are NeoForge-coupled), but ChemicalBuilder+ChemicalStack/ChemicalInstance can follow once the registry is in place. Access widener may need a new entry if Chemical.REGISTRY lookup methods require widening.
Coupling blockers: FluidStack import (net.neoforged.neoforge.fluids.FluidStack) in SerializerHelper line 29 — only 3 lines depend on it (LENIENT_OPTIONAL_FLUID_CODEC definition); Chemical.java imports datamaps (mekanism.api.datamaps.IMekanismDataMapTypes line 10, chemical attribute classes lines 11-12), radiation (mekanism.api.radiation.IRadiationManager line 13), and NeoForge DataMapType (net.neoforged.neoforge.registries.datamaps.DataMapType line 32) — these are NeoForge-only; MekanismAPI registry fields use NeoForge RegistryBuilder and DeferredHolder (imports lines 14-15) — must stay in :neoforge unless replaced with Architectury DeferredRegister; MekanismAPITags.Chemicals inner class uses MekanismAPI.CHEMICAL_REGISTRY_NAME (lines 101, 162) — depends on registry key existing in MekanismAPIBase
Hoistable now: SerializerHelper.java minus LENIENT_OPTIONAL_FLUID_CODEC (lines 88-90) — all other codecs are vanilla/minecraft-only; MekanismAPITags.java — fully loader-neutral; uses only vanilla Registries, TagKey, and MekanismAPI.MEKANISM_MODID (from MekanismAPIBase); ResourceKey<Registry<Chemical>> CHEMICAL_REGISTRY_NAME constant definition (MekanismAPI lines 36); ResourceKey<Chemical> EMPTY_CHEMICAL_KEY constant definition (MekanismAPI line 43); registryKey() and codecRegistryKey() helper methods from MekanismAPIBase can remain and be used to define CHEMICAL_REGISTRY_NAME
Deferred: SerializerHelper LENIENT_OPTIONAL_FLUID_CODEC stays in :neoforge (FluidStack is NeoForge-only); Chemical.java stays in :neoforge entirely (uses datamaps, radiation manager, NeoForge DataMapType, and registry access methods); MekanismAPI.CHEMICAL_REGISTRY (the registry instance itself, not the name) must wait for Architectury ChemicalDeferredRegister creation; MekanismAPI.CHEMICAL_INGREDIENT_TYPES, MODULE_REGISTRY, ROBIT_SKIN_SERIALIZER_REGISTRY stay in :neoforge (RegistryBuilder-coupled); IHasTextComponent.IHasEnumNameTextComponent nested interface in :neoforge — references NeoForge TranslatableEnum (cannot be hoisted); IHasTranslationKey alone is already in :common; TextComponentUtil.translate() and EnumColor stay in :neoforge (TextComponentUtil exists only in :neoforge, not :common); Chemical.getTextComponent() calls it so Chemical must stay too
